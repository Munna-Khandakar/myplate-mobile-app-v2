export type PlateType = {
  title: string;
  description: string;
  images: string[];
  category: string;
  quantity: number;
  price: string; // Assuming price is stored as a string
  address: string;
};
